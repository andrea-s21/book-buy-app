export default function BookCard({ book }) {
    return (
      <div className="flight-card">
        {/* <p className="card-title">{book.title}</p> */}
        {/* <img className="logo" src={flights.logoURL} alt="logo" />
        <p className="card-title">{flights.phone}</p>
        <p className="card-title">{flights.site}</p>
        <p className="card-title">{flights.code}</p> */}
      </div>
    );
  }